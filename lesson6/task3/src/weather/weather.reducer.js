
const initialState = {
  weatherData: []
}

const weatherReducer = (state = initialState, action) => {
  switch (action.type) {

    default: {
      return state;
    }
  }
}

export default weatherReducer;